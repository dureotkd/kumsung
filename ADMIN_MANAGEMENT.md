# 관리자 계정 운영 가이드

## 권한 체계

| 권한 | 통합 업무 운영 | SHOP 제품·결제 설정 | 관리자 계정 관리 |
|---|---:|---:|---:|
| 일반관리자 (`ADMIN`) | 가능 | 불가 | 불가 |
| SHOP 관리자 (`SHOP_ADMIN`) | 불가 | 가능 | 불가 |
| 최고관리자 (`SUPER_ADMIN`) | 가능 | 가능 | 가능 |

일반관리자는 견적, 고객, 프로젝트, 계약, 납품, A/S, 문의, 공지 등 통합 운영 기능을 사용합니다. SHOP 관리자는 별도 `shop-admin.html`에서 제품 가격·이미지·공개 상태와 토스페이먼츠 모듈 설정만 관리합니다. SHOP 화면 진입 시에는 로그인 비밀번호가 아닌 별도 `SHOP_ADMIN_ACCESS_PASSWORD`를 아이디 없이 매번 입력해야 합니다. 최고관리자는 양쪽 관리자 화면에 접근하고 관리자 초대, 권한 변경, 활성화·비활성화, 비밀번호 재설정도 수행합니다.

## 최초 최고관리자

`ADMIN_EMAIL`, `ADMIN_PASSWORD`는 최초 최고관리자를 만들 때만 사용합니다. 같은 이메일의 관리자가 이미 존재하면 애플리케이션 재시작 시 비밀번호를 덮어쓰지 않습니다.

최초 로그인을 확인하면 환경 파일에서 두 값을 비우고 애플리케이션 컨테이너를 재생성합니다. 운영 Compose는 두 값이 비어 있어도 정상 기동하며 기존 최고관리자 계정은 유지됩니다.

기존 `ADMIN` 계정은 V5 데이터베이스 마이그레이션에서 `SUPER_ADMIN`으로 보존됩니다.

## 관리자 추가

1. 최고관리자로 관리자 페이지에 로그인합니다.
2. 왼쪽 메뉴에서 **관리자 관리**를 선택합니다.
3. 직원 이름, 회사 이메일, 권한을 입력합니다.
4. **초대 메일 발송**을 누릅니다.
5. 직원은 48시간 이내에 이메일 링크를 열고 자신만의 12자 이상 비밀번호를 설정합니다.
6. 설정이 끝나면 기존 로그인 화면에서 관리자 이메일로 로그인합니다.

`SHOP 관리자`로 초대된 직원은 로그인 후 SHOP 전용 2차 비밀번호 확인 화면으로 이동합니다. 아이디 없이 `SHOP_ADMIN_ACCESS_PASSWORD`만 입력하며, 확인 전에는 제품·주문·결제 설정 API가 차단됩니다. 통합 관리자 화면과 API에는 접근할 수 없습니다.

관리자 비밀번호는 최고관리자도 확인하거나 지정할 수 없습니다. 초대 토큰 원문도 DB에 저장하지 않고 SHA-256 해시만 저장합니다.

## 계정 변경

- 권한 변경: 관리자 목록의 권한 선택
- 비활성화: 상태 버튼 선택
- 재활성화: 비활성 계정의 상태 버튼 선택
- 비밀번호 초기화: **비밀번호 재설정** 버튼으로 2시간짜리 링크 발송
- 초대 취소: 대기 중인 초대 목록에서 **초대 취소**

계정 비활성화, 권한 변경, 비밀번호 재설정 완료 시 해당 관리자의 기존 로그인 세션이 만료됩니다.

## 안전장치

- 자기 자신의 권한 변경 및 비활성화 금지
- 활성 상태의 마지막 최고관리자 강등·비활성화 금지
- 고객 이메일과 관리자 이메일 중복 금지
- 초대 링크 48시간, 비밀번호 재설정 링크 2시간 제한
- 사용 완료·취소·만료 토큰 재사용 금지
- 관리자 계정 삭제 대신 비활성화 사용
- 관리자 관리 API는 최고관리자만 접근 가능
- 초대, 수락, 권한 변경, 상태 변경, 비밀번호 재설정, 초대 취소를 `audit_logs`에 기록
- 공개 계정 설정 API는 IP별 요청 횟수 제한 적용

## 메일 설정 주의사항

외부 SMTP를 설정하지 않고 Mailpit을 사용하는 동안에는 초대 메일이 실제 직원에게 전달되지 않고 `http://localhost:8025`에만 나타납니다. 실제 운영 전 [SMTP_SETUP.md](SMTP_SETUP.md)에 따라 회사 또는 외부 SMTP를 연결해야 합니다.
