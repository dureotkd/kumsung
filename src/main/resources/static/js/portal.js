const titles={dashboard:"대시보드",quotes:"견적관리",shop:"SMART SHOP 문의",projects:"프로젝트 관리",contracts:"계약관리",deliveries:"납품현황",invoices:"세금계산서",service:"A/S 요청",notices:"공지사항",support:"고객센터",privacy:"개인정보 관리"};
function notify(s){toast.textContent=s;toast.classList.add("show");setTimeout(()=>toast.classList.remove("show"),2500)}
function showView(id){if(!titles[id])return;document.querySelectorAll(".view").forEach(v=>v.classList.toggle("active",v.id===id));document.querySelectorAll(".side-nav button").forEach(b=>b.classList.toggle("active",b.dataset.view===id));viewTitle.textContent=titles[id];if(location.hash!==`#${id}`)history.replaceState(null,"",`#${id}`);if(id==="dashboard")dashboard();else loadView(id)}
document.querySelectorAll(".side-nav button").forEach(b=>b.onclick=()=>showView(b.dataset.view));
const empty=t=>`<p class="empty">${t}</p>`;
function table(rows,cols){if(!rows.length)return empty("등록된 내역이 없습니다.");return `<table class="table"><thead><tr>${cols.map(c=>`<th>${c[0]}</th>`).join("")}</tr></thead><tbody>${rows.map(r=>`<tr>${cols.map(c=>`<td>${typeof c[2]==="function"?c[2](r[c[1]],r):esc(r[c[1]])}</td>`).join("")}</tr>`).join("")}</tbody></table>`}
let quotes=[];
async function init(){const me=await api("/api/auth/me");if(me.role==="ADMIN"){location.replace(me.adminRole==="SHOP_ADMIN"?"/shop-admin-entry.html":"/admin.html");return}userName.textContent=me.name;companyName.textContent=me.companyName;const requested=location.hash.slice(1);await dashboard();if(requested&&requested!=="dashboard"&&titles[requested])showView(requested)}
async function dashboard(){const [s,q]=await Promise.all([api("/api/portal/summary"),api("/api/portal/quotes?limit=5")]);quotes=q;sQuotes.textContent=s.quotes;sActive.textContent=s.active;sProjects.textContent=s.projects;sMessages.textContent=s.messages;recentQuotes.innerHTML=quoteRows(q)}
function quoteRows(q){return table(q,[["접수번호","receipt_number",(v)=>`<button class="action" onclick="openQuote('${esc(v)}')">${esc(v)}</button>`],["제목","subject"],["제품/공종","product_type"],["상태","status",v=>`<span class="badge">${statusName[v]||v}</span>`],["접수일","created_at",fmt]])}
async function loadView(id){try{
if(id==="quotes"){const p=await apiPage("/api/portal/quotes","portal-quotes");quotes=p.items;renderPage(quoteTable,"portal-quotes",p,quoteRows(p.items),()=>loadView("quotes"))}
else if(id==="shop"){const p=await apiPage("/api/portal/shop-inquiries","portal-shop");renderPage(shopInquiryTable,"portal-shop",p,table(p.items,[["접수번호","receipt_number"],["제품","items"],["문의내용","message"],["상태","status",v=>`<span class="badge">${esc(v)}</span>`],["관리자 안내","admin_note"],["접수일","created_at",fmt]]),()=>loadView("shop"))}
else if(id==="projects"){const p=await apiPage("/api/portal/projects","portal-projects");const html=p.items.length?p.items.map(x=>`<article class="data-card"><span class="badge">${statusName[x.status]||esc(x.status)}</span><h3>${esc(x.name)}</h3><p>${esc(x.start_date)} ~ ${esc(x.end_date)}</p><div class="progress"><i style="width:${+x.progress||0}%"></i></div><p>진행률 ${+x.progress||0}%</p></article>`).join(""):empty("등록된 프로젝트가 없습니다.");renderPage(projectCards,"portal-projects",p,html,()=>loadView("projects"))}
else if(id==="contracts"){const p=await apiPage("/api/portal/contracts","portal-contracts");renderPage(contractTable,"portal-contracts",p,table(p.items,[["계약번호","contract_number"],["계약명","title"],["금액","amount",v=>Number(v||0).toLocaleString()+"원"],["상태","status"],["계약일","contract_date"]]),()=>loadView("contracts"))}
else if(id==="deliveries"){const p=await apiPage("/api/portal/deliveries","portal-deliveries");renderPage(deliveryTable,"portal-deliveries",p,table(p.items,[["품목","item_name"],["수량","quantity"],["예정일","expected_date"],["납품일","delivered_date"],["상태","status"]]),()=>loadView("deliveries"))}
else if(id==="invoices"){const p=await apiPage("/api/portal/tax-invoices","portal-invoices");renderPage(invoiceTable,"portal-invoices",p,table(p.items,[["발행번호","issue_number"],["금액","amount",v=>Number(v||0).toLocaleString()+"원"],["발행일","issued_date"],["상태","status"]]),()=>loadView("invoices"))}
else if(id==="service"){const p=await apiPage("/api/portal/service-requests","portal-service");renderPage(serviceTable,"portal-service",p,table(p.items,[["제목","title"],["상태","status"],["접수일","created_at",fmt],["수정일","updated_at",fmt]]),()=>loadView("service"))}
else if(id==="notices"){const p=await apiPage("/api/public/notices","portal-notices");const html=p.items.length?p.items.map(x=>`<article class="notice"><h3>${x.pinned?"📌 ":""}${esc(x.title)}</h3><small>${fmt(x.created_at)}</small><p>${esc(x.content)}</p></article>`).join(""):empty("공지사항이 없습니다.");renderPage(noticeList,"portal-notices",p,html,()=>loadView("notices"))}
else if(id==="support"){const p=await apiPage("/api/portal/support","portal-support");renderPage(supportList,"portal-support",p,table(p.items,[["제목","subject"],["상태","status"],["문의일","created_at",fmt],["답변","answer"]]),()=>loadView("support"))}
}catch(e){notify(e.message)}}
async function openQuote(receipt){try{const q=await api(`/api/portal/quotes/${receipt}`);quoteDetail.innerHTML=`<div class="modal-head"><div><small>${esc(q.receipt_number)}</small><h2>${esc(q.subject)}</h2></div><button onclick="detailDialog.close()">×</button></div>
<h3>견적 정보</h3><div class="detail-grid"><div><span>상태</span>${statusName[q.status]||q.status}</div><div><span>담당자</span>${esc(q.assigned_to||"배정 중")}</div><div><span>현장</span>${esc(q.site_name||"-")}</div><div><span>제품/공종</span>${esc(q.product_type)}</div><div style="grid-column:1/-1"><span>요청사항</span>${esc(q.details)}</div></div>
<h3>진행 이력</h3><ul class="timeline">${q.history.map(h=>`<li><b>${statusName[h.status]||h.status}</b> ${esc(h.note||"")}<small>${fmt(h.created_at)}</small></li>`).join("")}</ul>
<h3>웹하드 공유</h3>${q.webhard_url?`<p><a class="action" href="${esc(q.webhard_url)}" target="_blank" rel="noopener">담당자 공유 자료 열기 ↗</a></p>`:""}
<form id="customerWebhardForm" class="form-inline"><input name="url" type="url" value="${esc(q.customer_webhard_url||"")}" placeholder="고객 웹하드 공유 주소 https://..."><button class="action">주소 저장</button></form>
${q.supplementalRequests.some(x=>x.status==="REQUESTED")?`<h3>보완 요청</h3>${q.supplementalRequests.filter(x=>x.status==="REQUESTED").map(x=>`<p class="file-row">${esc(x.request_text)}</p>`).join("")}`:""}
<h3>추가자료 업로드</h3><form id="suppForm"><label>PDF·이미지·CAD·엑셀 자료<input type="file" name="files" multiple required accept=".pdf,.jpg,.jpeg,.png,.dwg,.dxf,.xls,.xlsx,.zip"></label><button class="primary">추가자료 업로드</button></form>
<h3>첨부파일</h3>${q.attachments.length?q.attachments.map(f=>`<div class="file-row"><span>${esc(f.original_name)} (${(f.file_size/1048576).toFixed(2)}MB)</span><a class="action" href="/api/portal/quotes/${receipt}/files/${f.id}">다운로드</a></div>`).join(""):empty("첨부파일이 없습니다.")}
${q.estimate_amount!=null?`<p class="estimate-summary"><b>견적금액 ${Number(q.estimate_amount).toLocaleString()}원</b><br>${esc(q.estimate_notes||"")}</p>`:""}
<h3>견적서·계약서</h3>${q.documents.length?q.documents.map(d=>`<div class="file-row"><span>[${esc(d.document_type)}] ${esc(d.title)} · ${esc(d.approval_status)}${d.document_type==="CONTRACT"?` · 계약 ${esc(d.contract_decision)}`:""}</span><span><a class="action" href="/api/portal/quotes/${receipt}/documents/${d.id}">다운로드</a> ${d.document_type!=="CONTRACT"&&d.approval_status!=="APPROVED"?`<button class="action" onclick="approveDoc('${receipt}',${d.id})">전자승인</button>`:""} ${d.document_type==="CONTRACT"&&d.contract_decision==="PENDING"?`<button class="action" onclick="decideContract('${receipt}',${d.id},'ACCEPTED')">계약 수락</button><button class="action danger" onclick="decideContract('${receipt}',${d.id},'REJECTED')">계약 거절</button>`:""}</span></div>`).join(""):empty("등록된 문서가 없습니다.")}
<h3>담당자 메시지</h3><div id="messageList"></div><form id="messageForm" class="form-inline"><input name="message" placeholder="문의 또는 회신 내용을 입력하세요" required><button class="action">전송</button></form>`;detailDialog.showModal();loadMessages(receipt);
const sf=document.querySelector("#suppForm");if(sf)sf.onsubmit=async e=>{e.preventDefault();const button=sf.querySelector("button");button.disabled=true;const d=new FormData(sf);try{notify((await api(`/api/portal/quotes/${receipt}/files`,{method:"POST",body:d})).message);openQuote(receipt)}catch(x){notify(x.message)}finally{button.disabled=false}};
customerWebhardForm.onsubmit=async e=>{e.preventDefault();try{notify((await api(`/api/portal/quotes/${receipt}/webhard`,{method:"PUT",body:JSON.stringify({url:customerWebhardForm.url.value})})).message);openQuote(receipt)}catch(x){notify(x.message)}};
messageForm.onsubmit=async e=>{e.preventDefault();try{await api(`/api/portal/quotes/${receipt}/messages`,{method:"POST",body:JSON.stringify({message:messageForm.message.value})});messageForm.reset();loadMessages(receipt)}catch(x){notify(x.message)}}}catch(e){notify(e.message)}}
async function loadMessages(r){const m=await api(`/api/portal/quotes/${r}/messages`);messageList.innerHTML=m.length?m.map(x=>`<div class="message-row ${x.sender_role==="ADMIN"?"admin":""}"><small>${x.sender_role==="ADMIN"?"(주)금성이엔씨":esc(x.sender_email)} · ${fmt(x.created_at)}</small><p>${esc(x.message)}</p></div>`).join(""):empty("메시지가 없습니다.")}
async function approveDoc(r,id){if(!confirm("이 문서를 전자승인하시겠습니까?"))return;try{notify((await api(`/api/portal/quotes/${r}/documents/${id}/approve`,{method:"POST"})).message);openQuote(r)}catch(e){notify(e.message)}}
async function decideContract(r,id,decision){const accepted=decision==="ACCEPTED";const note=accepted?prompt("계약 수락 메모(선택)","계약 내용을 확인하고 수락합니다."):prompt("계약 거절 사유를 입력하세요.");if(note===null||(!accepted&&!note.trim()))return;if(!confirm(`계약서를 ${accepted?"수락":"거절"}하시겠습니까? 이 기록은 변경할 수 없습니다.`))return;try{notify((await api(`/api/portal/quotes/${r}/documents/${id}/contract-decision`,{method:"POST",body:JSON.stringify({decision,note})})).message);openQuote(r)}catch(e){notify(e.message)}}
async function exportPrivacy(){try{const data=await api("/api/portal/privacy/export");const url=URL.createObjectURL(new Blob([JSON.stringify(data,null,2)],{type:"application/json"}));const a=document.createElement("a");a.href=url;a.download="kumsung-my-data.json";a.click();URL.revokeObjectURL(url)}catch(e){notify(e.message)}}
async function deleteAccount(){const password=prompt("계정 삭제를 위해 현재 비밀번호를 입력하세요.");if(!password)return;if(!confirm("계정과 개인정보를 삭제합니다. 계속하시겠습니까?"))return;try{await api("/api/portal/privacy/account",{method:"DELETE",body:JSON.stringify({password})});location.href="/"}catch(e){notify(e.message)}}
async function openServiceDialog(){try{const projects=await api("/api/portal/projects?limit=200");serviceProject.replaceChildren(new Option("프로젝트 미지정",""));projects.forEach(project=>serviceProject.add(new Option(project.name,String(project.id))));serviceDialog.showModal()}catch(e){notify(e.message)}}
serviceForm.onsubmit=async e=>{e.preventDefault();const data=Object.fromEntries(new FormData(serviceForm));data.projectId=data.projectId?Number(data.projectId):null;try{await api("/api/portal/service-requests",{method:"POST",body:JSON.stringify(data)});serviceDialog.close();serviceForm.reset();resetPage("portal-service");loadView("service");notify("A/S 요청이 접수되었습니다.")}catch(x){notify(x.message)}};
supportForm.onsubmit=async e=>{e.preventDefault();try{await api("/api/portal/support",{method:"POST",body:JSON.stringify(Object.fromEntries(new FormData(supportForm)))});supportDialog.close();supportForm.reset();loadView("support");notify("문의가 접수되었습니다.")}catch(x){notify(x.message)}};
init().catch(e=>notify(e.message));
